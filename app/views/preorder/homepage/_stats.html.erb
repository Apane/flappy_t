 <div class="row">
  <div class="col-md-6">
   <div class="leftside">
     <div id="video">
     <% if Settings.use_video_placeholder %>
       <div id="video_image">
      <%= image_tag 'video_placeholder.png', size: '530x385', alt: 'video_place_holder' %>
       </div>
      <div id="player" class="hidden" style="display: none;">
        <object width="530" height="385">
          <param name="movie" value="<%= Settings.video_embed_url %>?autohide=1&showinfo=0&rel=0&autoplay=1" />
          <param name="allowFullScreen" value="true" />
          <param name="allowscriptaccess" value="always" />
          <embed src="<%= Settings.video_embed_url %>?autohide=1&showinfo=0&rel=0&autoplay=1" type="application/x-shockwave-flash" width="530" height="385" allowscriptaccess="always" allowfullscreen="true"></embed>
        </object>
      </div>
      <% else %>
      <iframe width='530' height='385' src="<%= video_url %>"></iframe>
    <% end %>

    <div class="fb-share-button" data-href="http://developers.facebook.com/docs/plugins/" data-type="button_count"></div>
  </div>
 </div>
</div>

<div class="col-md-6">
<div class="rightside">
<div class="flappy-summary">Flappy is not just a plush toy.
He’s wireless and controls the
game when you lift him up.</div>
  <div id="backing">
    <ul>
      <li class="stats" id="backers">
        <%= number_with_delimiter Order.backers, :delimiter => "," %>
        <span><%= Settings.primary_stat %></span>
      </li>
      <li class="stats">
        <%= number_to_currency Order.revenue, :precision => 0 %>
        <span>of <%= number_to_currency Settings.project_goal.to_f, :precision => 0 %></span>
      </li>

      <% if Settings.expiration_date.present? %>
        <li class="stats" id="days">
          <%= distance_of_time_in_words_to_now(Settings.expiration_date).gsub(/\D/, "") %>
          <span><%= distance_of_time_in_words_to_now(Settings.expiration_date).gsub(/\d/, "").gsub("about", "") %> left</span>
        </li>
      <% else %>
        <li class="stats" id="days">
            &infin;
            <span>days left</span>
          </li>
      <% end %>
    </ul>

    <% if Order.revenue.to_f < Order.goal.to_f %>
      <div id='progress_bg' class='small'>
        <div id='progress' class='' style='width: <%= Order.percent %>%;'>
        </div>
      </div>
    <% else %>
      <div id="progress_bg">
        <div id="progress">
        </div>
        <div id='progress_text'><%= Order.percent %>% <%= Settings.progress_text %></div>
      </div>
    <% end %>

    <div id="reserve_container">
      <p id="ship_date"><%= Settings.ships %></p>
      <a href="/preorder/checkout" class="blue_button reserve"><%= Settings.call_to_action %><p class="fifteen">for $15</p></a>
      <p id="price"><%= Settings.price_human %></p>
      <p><%= Settings.dont_give_them_a_reason_to_say_no %></p>
      <%= like_button %>
    </div>

  </div>
    <p class="pledge">
      This project will only be funded if at least $25,000 is pledged by Wed, Mar 5 2014 4:26 PM PST.
    </p>
  </div>
</div>

</div>


